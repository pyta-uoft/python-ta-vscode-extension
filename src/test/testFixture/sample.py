"""A minimal Python file used only to give the test workspace a *.py file
so the extension's `workspaceContains:*.py` activation event fires."""


def add(a: int, b: int) -> int:
    """Return the sum of a and b."""
    return a + b
